# 织见 K6 流畅性优化实施方案

交付对象：GPT‑5.6。用户已授权按本方案优化，重点是减少拖动、缩放、悬停、编辑过程中的卡顿和掉帧。本轮不增加摘录、报告、关系分析等产品功能。先改善现有架构，不默认更换框架、渲染引擎或升级 Electron。

## 1. 工作目录与交付范围

- 工作目录：`D:\Documents\WorkBuddy\Interest\织见-K6`。
- 已验证基础提交：`4d2fe3a`，K6 / 0.8.0，现有分支 `codex/k6-upgrade`。
- 原目录 `D:\Documents\WorkBuddy\Interest\织见` 曾被其他任务切换版本并清理文件，禁止在原目录直接实施本轮修改。
- 本方案编写时，K6 目录另有未跟踪的 `push-k6.js`，不属于本方案，不运行、覆盖、删除或提交它。
- 开工先读适用的 AGENTS.md，确认当前分支和差异；可从当前已验证提交创建 `codex/k6-performance` 分支。发现后续改动先保留并核对，不执行 reset/clean。
- 版本建议：功能和数据格式兼容时交付 K6.1 / 0.8.1；开工核对是否已被占用。保留 K6 安装包，新包使用独立目录。
- 只修改本轮需要的源码、性能工具、测试和说明。完成实测后再打包，不自动安装或覆盖用户数据。

主要源码均在 `2-新版本及其素材/src/js/`。网页入口为 `2-新版本及其素材/织见-思维关系板-K6.html`；桌面工程在 `2-新版本及其素材/desktop/electron/`。开工以实际构建配置确认入口。

## 2. 已核对的代码事实与待验证瓶颈

以下是代码检查结果，不代表已通过性能剖析确定各项占比。

| 位置 | 当前行为 | 优化切入点 |
|---|---|---|
| state.js / requestRender | 已用 requestAnimationFrame 合并绘制请求 | 保留它，排查同步 render 绕过调度的高频路径 |
| render.js / render | 每帧 rebuildIdMap、syncHistoryBtns，遍历元素并同步多处 DOM | 图索引按变化更新；绘制与界面同步分开 |
| render.js / inViewport | 普通节点已有视口裁剪，但仍需全量遍历、计算可见性和边界 | 缓存边界，统一聚焦等分支，再视实测增加空间索引 |
| editors.js / drawLinks | 遍历全部自由连线，计算曲线并绘制，没有视口提前剔除 | 缓存几何，裁剪路径，保留穿过视口的连线 |
| render.js / nodeDepth、isMindNodeVisible、subtreeCount | 分别追溯祖先或遍历子树 | 在结构/折叠变化时计算，不能每帧重复走树 |
| interaction.js / mousemove | 每次事件执行 hoverHit 和 hitMindLinkPoint | 每帧合并悬停计算，拖动时避免无关悬停扫描 |
| interaction.js / onPointerMove | 每次拖动事件重新收集后代；便签/材料还执行 localAvoid | 手势开始建立移动集合；避让只处理附近候选 |
| render.js / render 末尾 | 更新工具栏、状态、Dock、预览 DOM；相机交互已有部分跳过逻辑 | 变化驱动更新，避免布局读写混杂和停止时突发全量工作 |
| app.js / saveState | 全项目 JSON.stringify + 同步 localStorage 写入 | 测量阻塞，未变化跳过保存，避免自动保存打断手势 |
| k6.js / queueRecovery | 捕获全项目快照、读取附件，清理版本时 getAll 全部恢复记录 | 测量克隆和读取成本；清理只读元数据/键，避免加载旧附件 |

K6 已有文字度量缓存、真实小缩略图、后台打包、画布独立撤销和恢复中心，不重复开发或移除这些能力。

此前一次 5000 节点压力样本的热态 render 耗时约 118–137ms，首轮约 311ms。这是特定数据与环境下的 CPU 调用耗时，不能作为当前机器的正式基线，也不能等同于屏幕 FPS。必须重新建立对照。

## 3. 第一阶段：先建立可复现基线

新增可关闭的开发性能开关，默认关闭，不在每帧打印日志。使用固定容量缓冲区收集数据，操作结束后统一输出。工具应可独立运行，不污染真实项目。

记录以下数据：

1. 每帧：输入处理、索引/几何、节点、连线、DOM 同步、总绘制耗时。
2. 手势期间：rAF 间隔的 P50/P95/P99，超过 33.3ms、50ms、100ms 的次数；统计实际绘制次数。
3. 命中检测：耗时、检查候选数；绘制：总节点/边数量、可见数量、实际绘制数量。
4. 保存、撤销快照、恢复快照：调用耗时、数据量、发生时是否正在交互。
5. 若运行环境支持，记录 Long Tasks；否则使用性能录制工具补证。rAF 间隔与绘制耗时分别报告，不能伪称精确的显示器掉帧率。
6. 可取得时记录 JS 堆和资源数量；不能取得的数据写明不可用，不推算成实测。

固定测试数据至少包括：

| 场景 | 内容 | 检查重点 |
|---|---|---|
| 小图 | 500 节点，分支树，适量自由边 | 保证优化没有拖慢日常使用 |
| 常用大图 | 2000 节点，屏幕可见约 100–300 | 拖动/缩放/悬停主验收 |
| 稀疏超大图 | 5000 节点，屏幕可见约 100–300 | 屏幕外内容不应持续消耗大量时间 |
| 总览图 | 5000 节点，缩小至大部分可见 | 细节分级和密集连线成本 |
| 密集关系图 | 2000 节点、约 6000 条自由边 | 曲线裁剪、标签和命中成本 |
| 深层图 | 5000 节点的长链以及大量折叠分支 | 不栈溢出、不反复追溯整条祖先链 |
| 混合材料图 | 节点、长便签、笔迹、至少 50 个图片材料、数个预览窗 | 图片内存、DOM 重排、保存卡顿 |

每组执行：预热后平移 10 秒、缩放 10 秒、悬停 10 秒、拖单节点/含后代节点/多选集合，再测试松手后的 2 秒。至少 3 次，记录中位表现与最差一次。单独记录首次打开。

固定视窗大小、DPR、缩放级别、数据种子及操作轨迹。记录机器、显示刷新率、系统缩放和 Electron 版本。比较时不要一组开开发者工具、一组关闭。保存/恢复按时触发的样本单独标记，不能全部避开。

## 4. 第二阶段：合并高频工作与减少重复计算（最高优先级）

### 4.1 完善逐帧输入与渲染调度

- 高频入口统一请求下一帧，避免一帧内多次完整 render；保留确有同步需要的导出/截图路径。
- 悬停每帧只计算最新坐标。拖动和缩放合并工作时，必须保留累计位移与缩放增量，不能简单丢弃中间 wheel 事件。
- pointerup 前处理最后一个待处理位置，再提交历史与保存；处理 pointercancel、丢失捕获、窗口失焦，避免最终位置丢失或卡在拖动状态。
- 画笔保留轨迹点；若使用合并指针事件，需特性检测和回退，不能套用“仅取最后一点”。
- 拖动期间跳过无关 hoverHit，但保留建立连线所需的目标检测。空闲时无持续绘制循环。
- 为鼠标平移、触控板平移、捏合/滚轮缩放统一维护交互状态。当前 wheel 的恢复定时器为 80ms，部分注释写 200ms，应以行为为准统一，不靠随意拉长延迟掩盖卡顿。

### 4.2 拖动集合只建立一次

- pointerdown 时建立去重后的节点及后代集合、初始坐标和关联边列表。
- 收集后代使用迭代遍历和 visited，处理异常环与深树，避免递归栈溢出。
- pointermove 按起点计算绝对位移，不重复收集后代，不累加造成漂移。
- localAvoid 保留现有可见行为，先限制为邻近候选、每帧一次，并记录实际被推开的元素以保证撤销完整；暂不直接改成仅松手避让。
- 如果手势中发生结构变化或画布切换，显式刷新或取消手势，不能沿用旧对象。

### 4.3 为派生数据建立明确失效规则

可新增独立性能模块，复用现有脚本风格。至少区分结构变化、位置/尺寸变化、文字样式变化、可见性变化、相机变化和界面选择变化。

- idMap 在增删/导入/撤销/恢复/切板时重建，单纯平移缩放不重建。
- 按画布缓存深度、可见性、子树数量、邻接边；结构与折叠变化后重算相关数据。
- 边界和文字布局按元素缓存。文字、尺寸、字体加载、样式、层级及影响布局的设置变化必须失效；保留 _forcedDepth 语义。
- 不仅检查数组长度：同长度替换、对象替换、父子关系变化都必须生效。
- 绘制前缓存当前项目、画布、items、camera，减少循环内代理属性的重复查找。
- 所有派生缓存不进入保存、导出或恢复数据。
- 先逐一覆盖现有修改入口，再去掉每帧重建的安全兜底；不能先去掉兜底再猜测缓存是否正确。

阶段验收：相机平移不会重建全图索引；一次拖动只收集一次移动集合；快速松手的位置与预期一致；连续悬停不会按事件频率全图扫描。

## 5. 第三阶段：减少实际绘制和命中成本

### 5.1 补齐裁剪

- 普通模式、聚焦过渡、搜索高亮等绘制分支统一使用保守边界裁剪，保留描边、阴影、控制柄的屏幕空间外扩量。
- 自由连线、父子连线、连接器分别核对。依据实际路线的保守包围盒筛选：曲线可使用包含控制点的保守边界，折线路由包含全部折点，不能仅检测两端节点是否可见。
- 标签和箭头可能独立进入视口，边界必须包含它们；跨越屏幕但两端都在屏幕外的连线必须保留。
- 几何缓存按相关端点变化更新；若路线计算依赖缩放/样式，把对应依赖加入失效条件。

### 5.2 按实测决定空间索引

若全量扫描和命中仍占主要时间，使用简单空间网格或适合本项目的索引缩小候选范围。不要为了引入索引进行全项目重写。

- 绘制按视口查候选；命中按鼠标附近查询，再走原有精确判断。
- 保持原来的层级和命中优先级，索引返回顺序不能直接决定命中对象。
- 移动、缩放尺寸、删除、撤销后及时更新；相机移动不重建世界坐标索引。
- 超长边、长笔迹不能插入数万网格造成内存爆炸，可放独立大对象列表，配合保守边界再精确检测。
- 若索引收益不足或维护成本过高，保留已验证的缓存与裁剪，并报告数据。

### 5.3 缩小时减少细节，交互时降低特效成本

- 以屏幕投影尺寸决定是否绘制文字、附属图标、阴影及非选中关系标签，阈值从实际视觉测试确定。
- 选中、编辑、正在连接的对象保留必要文字和控制柄；数据、位置、命中语义不因简化而变化。
- 阈值加入迟滞，防止连续缩放时反复闪烁。
- 停止操作后恢复当前缩放级别应有的细节；不要在总览状态突然恢复全部微小文字造成尖峰。
- 优先减少重复阴影与不可读细节，不默认降低整个画布清晰度。导出保持完整质量，不继承交互简化状态。

## 6. 第四阶段：DOM、附件及保存尖峰

### 6.1 DOM 与预览

- 状态栏、撤销按钮、Dock 只在对应状态改变时更新，先检查各函数已有缓存，避免重复做。
- 预览位置和预览内容分开同步；相机变化不重新构建 PDF、Word、图片内容。
- DOM 尺寸读与样式写分批执行；缓存稳定尺寸，必要时用 ResizeObserver 失效，避免每帧 scrollWidth/offsetHeight 触发重排。
- 交互结束时不要一次性重建全部预览内容。若分批恢复，优先可见内容，保证输入和选中状态不中断。
- 延续已有真实缩略图机制。先测解码峰值与资源生命周期，再采用有限并发、可见优先加载和有上限的缓存。
- 释放 bitmap/object URL 时确认没有其他预览仍在使用；关闭预览、切项目和清缓存分别验证。禁止无限缓存整张大图。

### 6.2 保存、恢复与历史

- 分别测 JSON.stringify、localStorage 写入和恢复快照克隆耗时，不能把“防抖”当成消除了同步阻塞。
- 先加入脏状态/修订号：无变化跳过周期存档；相机变化也是需要保存的状态，但可合并频繁写入。
- 自动保存与自动恢复在交互期间可短暂推迟，但必须设置最大延迟，持续操作也不能无限不保存。初始最大延迟可取 5 秒，结合实测调整。
- 保存状态必须反映真实完成情况；失败仍显示错误，退出时继续执行可靠的最终 flush。删除前恢复点属于正确性要求，不随意推迟。
- 恢复版本清理只读取键、时间等必要信息，避免 getAll 拉回全部历史附件；恢复中心先展示元数据，选择具体版本后再读完整内容。
- 检查历史快照是否在开始拖动时造成长任务。如果显著，单独提出并实施经测试的修复；本轮不直接把全部撤销改写为操作日志。
- requestIdleCallback 仅改变调度时机，不会把同步 stringify 变为后台工作。Worker 也有快照复制成本，必须计入。
- 若上述优化后存储仍造成明显长任务，再单独设计异步持久化迁移：兼容旧数据、事务提交、修订顺序、崩溃恢复、失败回退、退出等待。不要顺带替换存储架构。

## 7. 验收目标与回归要求

以下为同一测试机、60Hz 环境的初始目标，不是预先承诺。先报基线，再对照实测；未达标写明差距，不以平均 FPS 遮盖长卡顿。

| 场景 | 目标 |
|---|---|
| 500 节点 | 高频路径 P95 不出现超过 10% 的稳定退步；无新增 >100ms 停顿 |
| 2000 节点、可见 100–300 | 热态绘制工作 P95 ≤12ms；交互 rAF 间隔 P95 ≤25ms |
| 5000 节点、可见 100–300 | 热态绘制工作 P95 ≤16ms；交互 rAF 间隔 P95 ≤33.3ms |
| 5000 节点总览/密集关系 | 相同样本绘制 P95 较基线至少下降 50%；争取 rAF 间隔 P95 ≤33.3ms，未达成独立报告 |
| 悬停 | 常用大图命中计算 P95 ≤4ms，鼠标反馈无明显延迟 |
| 交互结束 | 松手/停滚后的恢复工作不产生新增 >100ms 停顿 |
| 空闲与资源 | 静止无持续画布重绘；反复切板/开关预览后无单调增长的遗留缓存或资源 |

若显示器并非 60Hz，同时提供刷新率归一化结果。测试至少覆盖 DPR 1 与 2、浅深主题、普通及重阴影样式、100% 与总览缩放。

必须验证：

- 节点、后代、多选拖动；便签避让及撤销；调整尺寸；鼠标与触控板平移缩放；画笔快速转折；连线创建及命中。
- 最后指针位置、取消/失焦、切板时的手势清理。
- 折叠后代不可绘制或命中；聚焦/搜索恢复正确；穿屏连线不消失；层叠对象命中顺序正确。
- 修改字体/文本/父节点后缓存刷新；相同数量对象的撤销替换后索引正确；异常循环与深树不死循环。
- 画布独立撤销、跨项目移动、附件保存、导入导出、恢复副本、退出等待等 K6 能力保持正常。
- 长时间交互期间最终数据确实保存；写入失败仍可见；新任务不让旧异步结果覆盖新状态。

现有回归入口：`node --test "2-新版本及其素材/tests/k6.test.cjs"`，另有 `tests/electron-smoke.cjs`。使用独立临时用户数据目录，先核对烟测脚本中是否存在旧构建路径。

新增测试应覆盖缓存失效、最后输入刷新、连线裁剪、索引命中顺序等真正可能出错的行为。性能阈值放本机基准报告，不放环境波动很大的硬性单元测试中。

## 8. 执行顺序、预算控制与最终交付

按以下小批次推进，每批有实测结果后提交；测试通过后不无理由重复全量检查。

1. 基线工具与测试数据：输出当前瓶颈排名。
2. 输入合并、移动集合、图索引与树缓存：优先削减重复 CPU 工作。
3. 连线裁剪、边界缓存、细节分级；空间索引按数据决定。
4. DOM 和保存/恢复尖峰：解决停止操作后卡一下的问题。
5. 回归、最终桌面实测、独立目录打包、性能结果说明。

每批记录修改、数据变化、风险与剩余问题。达到目标后停止扩展架构，不开展 WebGL、OffscreenCanvas 全量迁移、全新状态管理等大改；若确有必要，先给瓶颈证据和单独方案。

最终交付：

- 可运行源码与分批提交。
- 同条件优化前后表：P50/P95/P99、长帧次数、关键路径耗时、场景规模、可见数量和运行环境。
- 可复现性能脚本、测试数据生成方法及测试报告；正式安装包与便携包在单独版本目录。
- 已验证的性能收益、仍未达到的目标、兼容性说明和回退方法。
- 没有执行的测试明确标记，不能用静态检查代替桌面拖动/缩放实测。

用户已授权按此计划优化，不需要逐项再问是否开始。遇到真实数据迁移、不可逆覆盖等超出本轮范围的事情，先完成可审阅的设计与证据再说明。默认本任务独立完成，不另行创建任务或启动子代理。
