const fs=require('fs'),path=require('path'),crypto=require('crypto');
const base=path.resolve(__dirname,'..'),skill=path.resolve(base,'../.agents/skills/zhijian-ai');
const read=name=>fs.readFileSync(path.join(skill,name),'utf8').replace(/^\uFEFF/,'');
const main=read('SKILL.md'),schema=read('references/schema.md'),api=read('references/api.md'),parser=read('scripts/parse-fantin.js');
const report=main.slice(main.indexOf('## 读取 .fantin'))+'\n\n'+schema+'\n\n## 完整解析器\n\n~~~js\n'+parser+'\n~~~\n';
const full=main+'\n\n'+api+'\n\n'+schema+'\n\n## 完整解析器\n\n~~~js\n'+parser+'\n~~~\n';
fs.writeFileSync(path.join(base,'src/js/ai-guide.js'),'/* Generated by scripts/build-ai-guide.cjs; edit canonical skill files. */\nconst ZHIJIAN_AI_GUIDE='+JSON.stringify({version:'2.0',full,report})+';\n');
const files={};for(const name of ['SKILL.md','references/schema.md','references/api.md','scripts/parse-fantin.js'])files[name]=crypto.createHash('sha256').update(fs.readFileSync(path.join(skill,name))).digest('hex');
fs.writeFileSync(path.join(skill,'managed-manifest.json'),JSON.stringify({version:'L1',files},null,2)+'\n');
console.log('L1 AI guide and manifest generated');
